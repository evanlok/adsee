<%= simple_form_for [:admin, @song_category] do |f| %>
  <%= f.input :name %>

  <%= f.submit 'Save', class: 'btn btn-primary' %>
  <%= link_to 'Cancel', admin_song_categories_path, class: 'btn btn-default', target: '_self' %>
<% end %>
