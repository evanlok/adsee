<h1 class="page-header">Theme Variants</h1>

<p>
  <%= link_to 'New Theme Variant', new_admin_theme_variant_path %>
</p>

<table class="table">
  <thead>
  <tr>
    <th>ID</th>
    <th>Theme</th>
    <th>Video Type</th>
    <th colspan="2">Scenes</th>
  </tr>
  </thead>

  <tbody>
  <% @theme_variants.each do |theme_variant| %>
    <tr>
      <td><%= theme_variant.id %></td>
      <td><%= link_to theme_variant.theme.name, [:edit, :admin, theme_variant] %></td>
      <td><%= theme_variant.video_type.name %></td>
      <td><%= theme_variant.scenes.map(&:name).join(', ') %></td>

      <td class="actions">
        <%= link_to [:edit, :admin, theme_variant], title: 'Edit' do %>
          <i class="fa fa-pencil"></i>
        <% end %>

        <%= link_to [:admin, theme_variant], method: :delete, title: 'Delete', data: { confirm: 'Are you sure?'} do %>
          <i class="fa fa-trash-o"></i>
        <% end %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= paginate @theme_variants %>
