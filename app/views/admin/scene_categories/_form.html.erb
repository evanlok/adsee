<%= simple_form_for [:admin, @scene_category] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>

  <%= f.submit 'Save', class: 'btn btn-primary' %>
  <%= link_to 'Cancel', admin_scene_categories_path, class: 'btn btn-default', target: '_self' %>
<% end %>
